import SwiftUI

struct Messenger_View: View {
    var body: some View {
        // we were planning on using rocket.chat
        // database needs to be integrated and store the messages
        ZStack{
            Color.white
            Image(systemName: "message.circle.fill")
                .foregroundColor(Color.black)
                .font(.system(size:100.0))
        }
    }
}

struct Messenger_View_Previews: PreviewProvider {
    static var previews: some View {
        Messenger_View()
    }
}
